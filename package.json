{
  "name": "staticchaos",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "doc"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "mud": "npm run build -- --pretty false && node dist/mud.js",
    "mud:dev": "npm run build -- --pretty false && node dist/mud.js start dev",
    "mud:prod": "npm run build -- --pretty false && node dist/mud.js start prod",
    "mud:client": "npm run build -- --pretty false && node dist/mud-client.js",
    "mud:bridge": "npm run build -- --pretty false && node dist/bridge.js",
    "mud:dev:watch": "npm run build -- --pretty false && node dist/mud-watch.js dev",
    "mud:prod:watch": "npm run build -- --pretty false && node dist/mud-watch.js prod",
    "docker:build": "docker compose build",
    "docker:up:dev": "docker compose up --build mud-dev",
    "docker:up:prod": "docker compose up --build mud-prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mjshuff23/staticchaos.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/mjshuff23/staticchaos/issues"
  },
  "homepage": "https://github.com/mjshuff23/staticchaos#readme",
  "devDependencies": {
    "@types/node": "^25.2.3",
    "typescript": "^5.9.3"
  }
}
